# 🎬 AI-Studio Sora Auto Video Generator (ENV 버전)

Flask 기반으로 Sora API를 이용해 영상 생성, 컷 자동 분할, 워터마크 흐림 후처리를 수행합니다.
.env 파일을 로드해 API 키를 안전하게 관리합니다.

## 🧩 실행 방법

### 1. 환경 준비
```bash
pip install -r requirements.txt
sudo apt install ffmpeg
```

### 2. .env 파일 생성
```bash
SORA_TOKEN=your_openai_sora_api_key
PLAN=PLUS
```
※ `.gitignore`에 `.env`를 추가해 깃허브에 올리지 마세요.

### 3. 서버 실행
```bash
python app.py
```

### 4. 브라우저 접속
```
http://localhost:8080
```

---

## 🪄 자동 처리
- 컷 분할: 구독 플랜(PLUS/PRO)에 따라 10~25초 단위 자동 분할
- remix_id + reference_inputs: 캐릭터·배경·음성 일관성 유지
- OpenCV + FFmpeg boxblur: 워터마크 영역만 흐림
- output_final.mp4 자동 다운로드

---

## 🐳 Docker 실행
```bash
docker build -t ai-studio-sora .
docker run -p 8080:8080 --env-file .env ai-studio-sora
```

---

## ⚠️ 주의
- 워터마크 제거가 아닌 **부분 흐림(masking)** 만 수행합니다.
- 상업적 사용 시 반드시 “AI generated by OpenAI Sora” 문구를 포함하세요.
